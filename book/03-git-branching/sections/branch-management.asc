[[_branch_management]]
=== Zarządzanie gałęziami

(((branches, managing)))
Po utworzeniu, scaleniu i usunięciu niektórych gałęzi rzućmy okiem na gałąź.

Polecenie `git branch` nie tylko tworzy i usuwa gałęzie.(((git commands, branch)))
Jeśli uruchomisz go bez argumentów, otrzymasz prostą listę bieżących oddziałów:
[source,console]
----
$ git branch
  iss53
* master
  testing
----
Zwróć uwagę na znak `*`, który prefiksuje gałąź `master`: wskazuje gałąź, którą aktualnie wypisałeś (tj. Gałąź, do której wskazuje` HEAD`).
Oznacza to, że jeśli dokonasz zatwierdzenia w tym momencie, gałąź `master` zostanie przeniesiona do przodu z twoją nową pracą.
Aby zobaczyć ostatnie zatwierdzenie w każdym oddziale, możesz uruchomić `git branch -v`:
[source,console]
----
$ git branch -v
  iss53   93b412c fix javascript issue
* master  7a98805 Merge branch 'iss53'
  testing 782fd34 add scott to the author list in the readmes
----
Przydatne opcje `--merged` i` --no-merged` mogą filtrować tę listę do gałęzi, które masz lub jeszcze nie połączyłeś z oddziałem, w którym aktualnie się znajdujesz.
Aby zobaczyć, które gałęzie są już scalone z gałęzią, na której jesteś, możesz uruchomić `git branch --merged`:
[source,console]
----
$ git branch --merged
  iss53
* master
----

Ponieważ już wcześniej scaliłeś `iss53` wcześniej, widzisz go na liście.
Oddziały na tej liście bez `*` przed nimi są zazwyczaj dobrze usuwane z `git branch -d`; już włączyłeś ich pracę do innej gałęzi, więc nic nie stracisz.

Aby zobaczyć wszystkie gałęzie zawierające prace, których jeszcze nie połączyłeś, możesz uruchomić `gałąź git --no-merged`:

[source,console]
----
$ git branch --no-merged
  testing
----

To pokazuje twój inny oddział.
Ponieważ zawiera on pracę, która nie jest jeszcze scalona, próba usunięcia jej za pomocą `git branch -d` zakończy się niepowodzeniem:

[source,console]
----
$ git branch -d testing
error: The branch 'testing' is not fully merged.
If you are sure you want to delete it, run 'git branch -D testing'.
----

Jeśli naprawdę chcesz usunąć gałąź i stracić tę pracę, możesz wymusić ją za pomocą `-D`, jak wskazuje pomocna wiadomość.
